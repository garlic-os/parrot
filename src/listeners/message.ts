// REMINDER
// When implementing Parrot's learning behavior,
//   take advantage of purpl-markov-chain's chain.update() method.

import type { Message } from "discord.js";

export default (message: Message): void => {
    // TODO
};
